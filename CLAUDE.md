# Sleuth

Runtime performance diagnostics package for Flutter mobile apps. 23 detectors across 4 lifecycle types (runtime, vmOnly, hybrid, structural).

## Commands

```bash
# Always use fvm for all Flutter/Dart commands
 fvm flutter test                    # Run all tests (~2,417 tests, ~30s)
fvm flutter test test/detectors/    # Run detector tests only
fvm flutter analyze                 # Static analysis (must be 0 issues)
fvm flutter pub publish --dry-run   # Verify publish readiness

# Example app
cd example && fvm flutter run --profile   # Profile mode (recommended)
cd example && fvm flutter run             # Debug mode
cd example && fvm flutter test            # Cookbook smoke tests (9 tests)
```

## Architecture

```
lib/
  sleuth.dart          # Public API barrel file + Sleuth entry point
  src/
    models/                     # Data classes: PerformanceIssue, FrameStats, FrameVerdict, BaseDetector
    detectors/                  # 23 detector implementations (one file per detector)
    network/                    # HTTP monitoring: SleuthHttpOverrides, RequestRecord
    analyzer/                   # RenderPipelineAnalyzer, FrameEventCorrelator
    controller/                 # SleuthController (orchestrates detectors, config, scan loop)
    vm/                         # VmServiceClient, TimelineParser
    debug/                      # DebugInstrumentationCoordinator, DebugSnapshot
    ranking/                    # IssueRanker (weighted composite scoring)
    ui/                         # Overlay widgets: FloatingIssuesCard, IssueCard, AiChatPage, IssueEncyclopediaPage, TriggerButton, SleuthTheme
    utils/                      # WidgetLocation helper, FixHintBuilder, TypeNameCache
test/
    mirrors lib/src/ structure + helpers/ and benchmark/
```

### Key patterns

- **Detectors** extend `BaseDetector` (in `models/base_detector.dart`). Each has a `DetectorType` enum value and `DetectorLifecycle` (runtime, vmOnly, hybrid, structural). Built-in detectors implement 4 lifecycle methods (`prepareScan`, `checkElement`, `afterElement`, `finalizeScan`) for the unified tree walk. Custom detectors override `scanTree` directly.
- **SleuthController** owns all detectors, runs the scan loop (unified single-pass tree walk for all 16 tree-scanning detectors), and manages the `FrameVerdict` pipeline.
- **Three-tier verdict**: Correlated (VM timeline matched per-frame) > Full (VM batch) > Basic (FrameTiming only). Falls back automatically.
- Test helpers live in `test/helpers/` — `benchmark_helpers.dart` and `timeline_test_helpers.dart`.

## Conventions

- New detectors: add enum value to `DetectorType`, create detector file in `detectors/`, implement `prepareScan`/`checkElement`/`afterElement`/`finalizeScan` (not `scanTree`), register in `SleuthController`, add tests mirroring existing detector test structure. Tests call `scanTree` (the base class wrapper) — no special test setup needed.
- Issues use `IssueConfidence`: confirmed (directly observed), likely (runtime + structural), possible (structural only).
- Fix hints use `FixHintBuilder` (in `utils/fix_hint_builder.dart`) — never hardcode fixHint strings in detectors.
- All public API goes through `lib/sleuth.dart` barrel file.
- Package is completely disabled in release mode (`kReleaseMode` guard).

## Current state

- **v0.16.3** (current) — Third v0.16 milestone: four structural detectors raised from `EvidenceTier.unvalidated` to `EvidenceTier.reproducerOnly` in a single release (`ImageMemoryDetector`, `OpacityDetector`, `ListviewDetector`, `GlobalKeyDetector`). Pure-structural detectors chosen because their trigger paths are fully exercisable from `flutter_test` widget tests — no profile-mode capture, VM timeline, or decoded bitmap dependency — so the reliability contract is tractable in a single PR. **Added:** four hermetic reproducer files under `test/validation/`. (1) `image_memory_reproducer_test.dart` (+6 tests) pins `uncached_images` via a 40×40 / 50×50 / 51×51 / 100×100 boundary triad proving the 50dp small-image skip is inclusive at exactly 50dp and fires at 51dp, plus `ResizeImage`-wrapper suppression and the "zero is NOT small" unconstrained-size policy. Uses a 58-byte minimal-valid 1×1 transparent PNG via `MemoryImage`; decode completion is irrelevant because the detector is a pure structural scan over widget shape + render-object size. (2) `opacity_reproducer_test.dart` (+5 tests) pins `opacity_zero`: exact-zero contract (0.0 fires, 0.005 and 0.5 do not, per the v9.1 detector docstring), `AnimatedOpacity(0.0)` settled fires exactly once so the inner `FadeTransition` is suppressed by the `_insideAnimatedOpacity` depth counter (AnimatedOpacity internally builds a FadeTransition — naive counting would double-report), nested `Opacity(0.0)` produces one rollup issue whose title reflects both occurrences. (3) `listview_reproducer_test.dart` (+8 tests) pins 3 of 8 stable-id families: `non_lazy_listview` (childThreshold boundary + `ListView.builder` lazy-path bypass because `SliverChildBuilderDelegate` is NOT `SliverChildListDelegate`), `sliver_to_box_adapter_large`, `sliver_fill_remaining_scrollable` (eager-build trap with `hasScrollBody: false` vs happy path with `hasScrollBody: true`). Remaining 5 families (`non_lazy_gridview`, `non_lazy_sliver_list`, `non_lazy_sliver_grid`, `sliver_to_box_adapter_shrinkwrap`, `non_lazy_list`) remain implicitly `unvalidated` — same single-family-pin precedent as v0.16.1 NetworkMonitor, called out in the ledger row. (4) `global_key_reproducer_test.dart` (+7 tests) pins both families: `excessive_global_keys` (strict greater-than threshold, critical above 3× threshold, scrollable-context gate — bare-tree keys ignored because the detector only counts keys while a `ListView`/`GridView`/`PageView` is on its internal scrollable stack) and `global_key_recreation` (two scans with fresh `GlobalKey()` instances on the same scan root fires via min(newKeys, goneKeys) ≥ recreationThreshold, two scans reusing stable State-held GlobalKey instances do not, first scan alone is silent because `_prevKeyIds` is empty). `coveredStableIds` uses the canonical prefix form `excessive_global_keys` per the ledger's prefix convention for the indexed `excessive_global_keys:<i>` emissions. **Changed:** four `validationMetadata` getters updated in lockstep (`tier` raised to `reproducerOnly`, `reproducerPath` set, `coveredStableIds` declared, rationale rewritten to describe what each reproducer validates). `doc/validation_ledger.md` summary flipped `1 / 23 at reproducerOnly, 22 / 23 at unvalidated` → `5 / 23 at reproducerOnly, 18 / 23 at unvalidated`; four structural rows now link their reproducer files and describe what is pinned; roadmap line added; date bumped to 2026-04-21. No public API, schema, or dependency changes — patch bump (0.16.2 → 0.16.3) fits pre-1.0 v0.16 arc semver. Migration for external forks is zero. **Notes:** test count 2,391 → 2,417 (+26 reproducer tests); `fvm flutter analyze` clean; `fvm flutter test` all green; `fvm flutter pub publish --dry-run` clean. The detector-metadata audit gate now counts 5 `reproducerOnly` entries and enforces per-reproducer that the file exists, AST-gated `test(...)`/`testWidgets(...)` invocations reference the correct detector class via `SimpleIdentifier` (not string literals or comments), and `coveredStableIds` is non-empty with each entry non-empty.
- **v0.16.2** — Validation infrastructure milestone extending v0.16.1's per-detector ledger with machinery to raise claims to `runtimeVerified` / `externallyCited`. **Added:** `ComponentMetadata` + `ComponentMetadataProvider` mixin (parallel to `DetectorMetadata` for non-detector components; full-field structural `==`/`hashCode` so `const` instances from different compilation units compare equal); `ValidatedComponentRegistry` (`@internal`) append-only singleton with explicit `static void registerMetadata()` dispatch from audit `setUpAll` — throws `StateError` on collision with both existing and incoming metadata in the message; `ProfileCaptureSchema` (`parse()` + `parseFile()` + `validateBracket()`) validating `sleuthMetadata` on DevTools Chrome Trace Event exports — required keys, pair-matched device/OS, pinned Flutter major.minor (pre-release `-1.0.pre` + build-metadata `+channel-stable` suffixes accepted, pin strict on major.minor), `expectedMagnitude.{min,observed,max}` numeric + all `> 0` + `min ≤ observed ≤ max` + non-empty `unit` string (AB-1 no-ops without it), ISO-8601 `captureDate` round-tripped via `toIso8601String()` to catch silent rollover (`2026-13-45` → `2027-02-14`), `traceEvents` ≥10 entries with recognised Chrome Trace `ph` phases (`B`/`E`/`X`/`b`/`e`/`i`/`I`/`M`/`C`/`s`/`f`/`t`), pre-decode duplicate-key scanner (RFC 8259 §4 last-write trap), UTF-8 BOM strip + CRLF normalisation; `validateBracket()` below/at/above triad with ±10% at-band AND provenance cross-check across `device`/`deviceOsVersion`/`flutterVersion` throwing `FormatException` on mismatch; AB-1 trace-vs-observed ratio invariant bound to `scenarioBeginMarker`/`scenarioEndMarker` instant pair (ph=`i`) not min/max across whole capture, `maxObservedToSpanRatio = 100`; shared audit-invariant module `test/validation/_support/audit_invariants.dart` with 5 invariants (rationale, tier-fields, citation URL, reproducer contract, capture schema) — citation URL parsed as http/https URI with authority and host rejected for RFC1918 (`10/8`/`172.16/12`/`192.168/16`) + link-local (`169.254/16`) + IPv6 `fe80::/10` / `fc00::/7` via `InternetAddress.tryParse` family discrimination, path-traversal defence via `p.canonicalize`, block-comment-aware citation stripping, AST-based reproducer gate via `package:analyzer` (`_ReproducerAstVisitor extends RecursiveAstVisitor` — `visitMethodInvocation` flips `hasTestInvocation` only on real `test(...)`/`testWidgets(...)`; `visitSimpleIdentifier` credits required tokens only as AST nodes not string literals/comments; `${test(...)}` interpolation correctly credited); SHA-256 pin on `anchor_devtools_export.json` via `_expectedAnchorSha256` (intentional swap = same-PR constant update); `test/validation/ledger_sync_test.dart` counting per-tier rows between `## Ledger` and `## Non-Detector Components` headings cross-checked against `detectorsForAudit` tier counts + summary denominator; filesystem-walk gates over `lib/src/` for `with ComponentMetadataProvider` classes and `lib/src/detectors/` for `extends BaseDetector` classes (skipping `_`-prefixed + `SimpleStructuralDetector`) asserting registration; `test/validation/profile_capture_schema_test.dart` (+27) + `profile_capture_schema_anchor_test.dart` (+3) + `component_metadata_audit_test.dart` (+9); 6 capture fixtures (anchor DevTools export shape-faithful synthetic, 3 dormant-bracket around 1000 ms threshold, 3 negatives `missing_device`/`bad_iso_date`/`min_gt_observed`) padded with 11 events each; `doc/reference_devices.md` pinned matrix (iPhone 13 mini iOS 17.6.1 / Pixel 7 Android 14 / Flutter 3.32.x) + annual rotation policy; `test/validation/captures/README.md` contributor checklist; `CONTRIBUTING.md` top-level guide. Component audit's `_expectedRegisteredComponents` empty for v0.16.2; invariants 2–5 exercised against synthetic `ComponentMetadata` today, invariant 1 (registration dispatch) wired but dormant until v0.16.6 first real component. **Changed:** `DetectorMetadata.profileCapturePath` (`String?`) → `profileCapturePaths` (`List<String>?`) — bracketing rule needs three captures per threshold; audit asserts EXACTLY 3 entries via `_expectBracketCaptures` at `runtimeVerified`/`externallyCited` AND runs every capture through `ProfileCaptureSchema.parseFile`. Public barrel exports `ComponentMetadata` + `ComponentMetadataProvider` + `ProfileCaptureSchema` (registry stays `@internal`). `package:meta ^1.15.0` promoted to direct dep; `package:analyzer ^6.0.0` added dev-only (not re-exported). **Migration:** zero in-tree callers used `profileCapturePath` in v0.16.1; forks change `profileCapturePath: 'x.json'` → `profileCapturePaths: ['x.json']`. Patch bump (0.16.1 → 0.16.2) — v0.16 line is pre-1.0 validation infrastructure. **Notes:** test count 2,238 → 2,391; `fvm flutter analyze` clean; `fvm flutter test` all green (example 9/9). Spec OQ1 (anchor-fixture provenance) + OQ4 (pair-matched device policy) resolved in-release; ledger-sync `else`-branch coverage deferred to v0.16.3.
- v0.16.1 — First per-detector validation milestone built on v0.16.0's methodology infrastructure. The goal is a public reliability ledger: each detector carries a `DetectorMetadata` record declaring its `EvidenceTier`, the rationale for the claim, and — for anything above `unvalidated` — a linked hermetic reproducer that a user can run themselves. v0.16.1 lands the audit gate and the first tier raise. **Added:** (1) `SleuthController.detectorsForAudit` `@visibleForTesting` getter filtering out user-authored `customDetectors` so the audit walks only the 23 built-in detectors this package's reliability ledger covers. (2) `test/validation/detector_metadata_audit_test.dart` CI audit gate: 5 invariants — (a) every built-in detector mixes in `DetectorMetadataProvider`; (b) metadata is non-null with a non-empty rationale; (c) tier-appropriate fields (anything at `reproducerOnly` or stronger requires a `reproducerPath`); (d) reproducer files actually exist on disk when the test runs from the package root; (e) `NetworkMonitorDetector` is pinned at `reproducerOnly` so a silent demotion fails the test. The gate was manually fired by stripping `OpacityDetector`'s mixin — the test failed with the exact expected error message listing the offending detector — then restored. (3) `test/validation/network_monitor_reproducer_test.dart` — two-layer hermetic reproducer for `NetworkMonitorDetector`. **Layer 1** exercises `processRecord()` at threshold boundaries (999 ms below slow, 1000 ms slow, 2999 ms warning, 3000 ms critical, 3001 ms critical) and the `criticalSlowThresholdMs > slowThresholdMs` assertion invariant. **Layer 2** exercises the full `SleuthHttpOverrides → _MonitoringHttpClient → _MonitoringRequest → RequestRecord → processRecord` pipeline against a loopback `HttpServer` with a controlled response delay, so the claim that the detector catches slow requests in the real pipeline (not just unit-tested at the numerical boundary) is provable from a single `fvm flutter test` invocation. **Changed:** 22 detectors bulk-seeded with `with DetectorMetadataProvider` at `EvidenceTier.unvalidated` (lazy, rationale-carrying starting-line state — not a reliability claim, a contract that the audit gate now enforces going forward). `NetworkMonitorDetector` raised to `reproducerOnly` as the first tier raise, with `reproducerPath` pointing at the hermetic test. `DetectorMetadataProvider.validationMetadata` docstring reconciled with the F1 fix from v0.16.0 — detectors implement it as an instance getter backed by a `const` literal, and the audit walks the `SleuthController`'s registered detector instances (not reflection on static members, which Dart does not expose on the class interface). Test benchmark file `test/benchmark/scan_overhead_test.dart` now carries an explicit `List<BaseDetector>` annotation because the bulk-mixin change altered the inferred LUB of the heterogeneous detector list to `Object`, which broke `.scanTree(context)` dispatch. **Fixed:** subtle Dart `Stream.drain()` + `subscription.asFuture()` interaction surfaced during Layer 2 implementation — `drain()` internally calls `subscription.onDone(...)` which REPLACES the onDone handler passed at `listen()` time, so proxy-style monitoring wrappers that depend on their wrapping `onDone` callback to emit a `RequestRecord` silently lose the event. The reproducer now consumes the response via `await for (final _ in response)` instead of `.drain()`, with an inline comment explaining why. Production callers inside `_MonitoringResponse` that use `drain()` lose monitoring on that single request — documented as a known limitation; a proper fix (detect-drain + wire an alternative completion signal, or bypass proxy-via-drain entirely) is deferred to a follow-up milestone because it widens scope beyond the per-detector validation ledger this release is about. **Notes:** test count 2,225 → 2,238 (+8 reproducer + 5 audit gate). The audit gate now fires meaningfully on a realistic detector population, the way the v0.16.0 spec promised it would after the first per-detector validation PR. Subsequent v0.16.N milestones each raise one more detector's tier and land its reproducer, extending the public ledger one row at a time.
- v0.16.0 — Validation methodology infrastructure milestone. Ships the contract Sleuth will use to certify each detector threshold and feature heuristic, not validation of any individual detector. Detector-by-detector validation rolls through follow-up milestones (v0.16.1…v0.16.N), each raising one detector's `EvidenceTier` with a linked reproducer. **Added:** `lib/src/validation/evidence_tier.dart` (frozen 4-tier enum: `unvalidated`, `reproducerOnly`, `runtimeVerified`, `externallyCited`, ordered weakest-to-strongest — new tiers require a semver major bump) and `lib/src/validation/detector_metadata.dart` (`DetectorMetadata` data class + `DetectorMetadataProvider` mixin). Spec: `doc/spec_v0_16_validation_methodology.md`. **Fixed (HEAD bugs surfaced by the methodology-plan adversarial review):** (C1) `FrameTimingDetector` warmup is now duration-gated — `warmupDuration` constructor parameter (default 3 s) is AND-combined with `warmupFrameCount`. The v0.15.5 default of `warmupFrameCount: 180` silently under-warmed on 120 Hz devices (180 frames = 1.5 s, not the intended 3 s). `SleuthConfig.frameTimingWarmupDuration` carries the new default and `frameTimingWarmupFrameCount` drops to 0 so the duration gate drives warmup. (C2) Per-detector exception isolation widened from `checkElement`/`afterElement` to all five scan-loop stages (`prepareScan`, `checkElement`, `afterElement`, `notifyWalkCompleted`, `finalizeScan`, plus the legacy `scanTree`) — one misbehaving detector no longer halts the unified walk. (C3) VM reconnect ladder extended from `[1 s, 2 s, 4 s]` (cumulative 7 s) to `[1 s, 2 s, 4 s, 8 s, 16 s]` (cumulative ~31 s) to match the ~30 s reconnect window the docstring already advertised — improves recovery on real-device runs where USB/WiFi hiccups can drop the VM for > 7 s. (C4) `PerformanceIssue.fromJson` defensive casts on `topAllocators`/`rankingScore`/`rankingBreakdown`/`downstreamIds` matching the existing `scaffoldHashKey`/`tabVisitIndex` `is`-guard pattern — malformed payloads (JS 53-bit number coercion, schema drift) drop offending entries to null/empty-list instead of throwing. **Deferred (to follow-up milestones):** `test/validation/detector_metadata_audit_test.dart` CI audit gate (walks every concrete `BaseDetector` and asserts non-null metadata) — landing with the first per-detector validation PR so it fires meaningfully rather than on a 23-detector `unvalidated` seed commit. Per-detector metadata seeding happens alongside each tier raise, not as a bulk change. **Post-Codex hardening (3 fixes in same release):** Codex post-impl review returned `needs-attention` on the working-tree diff with three findings, all landed before tag/push. (Codex-1, high) `FrameTimingDetector` warmup gate now reads monotonic engine microseconds (`FrameTiming.vsyncStart`) rather than per-callback `DateTime.now()` — the initial C1 fix was vulnerable to `addTimingsCallback` batching collapsing many frames' timestamps to a single tick, which could keep the 3 s gate closed far past 3 s of engine time on cold start. New `_firstFrameVsyncUs` field seeded from the first frame's vsync µs + `@visibleForTesting handleTimingsForTest(List<FrameTiming>)` bridge to drive the real `_onTimings` pipeline from unit tests. Wall-clock path retained as fallback for synthetic test frames that lack `vsyncStartUs`. (Codex-2, medium) `PerformanceIssue.fromJson` now hardens the *nested* `topAllocators` entries — the initial C4 guard only checked outer List shape, so `{'topAllocators': [{'className': 42}]}` still crashed inside `AllocationEntry.fromJson`'s strict casts. New top-level `_tryParseAllocationEntries` helper wraps each entry parse in try/catch; malformed entries drop silently, valid entries survive alongside them. (Codex-3, medium) `EvidenceTier` + `DetectorMetadataProvider` now exported from `lib/sleuth.dart` — the headline v0.16.0 contract was only reachable via `package:sleuth/src/...`. New `test/validation/validation_public_api_test.dart` smoke test imports ONLY the public barrel so a missing export fails a test instead of shipping silently. **Post-Codex meta-review hardening (3 more fixes, same release):** a `/adversarial-review` pass over the three Codex post-impl fixes surfaced three residual findings that Codex concurred with on meta-review. All landed before tag/push. (Meta-C3, medium) `PerformanceIssue.fromJson` required enum fields (`severity`, `category`, `confidence`) + optional enum fields (`observationSource`, `interactionContext`, `fixEffort`) were still doing raw `IssueSeverity.values.byName(json[…] as String)` — a payload with `severity: 42` or `severity: 'warn'` aborted the whole snapshot, exactly the schema-drift failure mode Codex-2 exists to survive, just on different fields. New top-level `_tryParseEnum<T>` helper routes all six enum fields through guarded parsing; required fields fall back to neutral defaults (`IssueSeverity.warning`, `IssueCategory.build`, `IssueConfidence.possible`), optional fields drop to null. (Meta-C2, medium) `_tryParseAllocationEntries` narrowed from bare `catch (_)` to `on TypeError catch (_)` + `on Exception catch (_)` — catches the realistic schema-drift shapes (JS cast failures, `FormatException`, `ArgumentError`) but lets `StackOverflowError`, `OutOfMemoryError`, `StateError`, and other programmer-bug / VM-instability errors propagate so regressions surface with a stack trace instead of as silent missing allocator data. (Meta-C4, low) `EvidenceTier` ordering fully pinned — the Codex-3 smoke test asserted only 2 of 4 ordinals (`unvalidated.index == 0` + `externallyCited.index == 3`); a future reorder swapping the two middle values would have passed. Strengthened to pin full name sequence AND full ordinal sequence so any rename or reorder fails a test. **Post-advanced-adversarial-review hardening (2 more fixes, same release):** a final `/advanced-adversarial-review` (Claude ↔ Codex triangulation) converged in 4 substantive rounds on two residual blockers. Both landed before tag/push. (F1, high) `DetectorMetadataProvider` docstring claimed detectors would "implement this via a static `metadata` field so the audit test can reflect on types without constructing instances" — Dart does not treat static members as part of a class interface, so the audit-without-construction path is unimplementable. Replaced the docstring with the honest contract: `validationMetadata` is an instance getter backed by a `const` literal, the audit walks the `SleuthController`'s registered detector instances (same construction path the runtime exercises), and detector constructors must be side-effect-free so the audit can construct them in isolation. (F3, medium, combined with quarantine) Stage wrappers in `_runStructuralScans` used `assert(() { debugPrint(…); return true; }())` which profile-mode strips — so detector failures were silent in Sleuth's primary runtime target — AND a detector that threw in `prepareScan` was still called in every subsequent stage, potentially throwing `LateInitializationError` on half-initialised state or amplifying noise across thousands of visitor invocations. Combined fix in one pass: (a) replaced all seven assert+debugPrint blocks with `FlutterError.reportError(FlutterErrorDetails(…))` which survives profile-mode compilation and integrates with the app's crash reporter, and (b) added a per-scan `Set<BaseDetector> failedDetectors` consulted at the top of every per-detector stage (including inside the visitor closure). **F2 disposition:** enum-drift cascade held as a user decision between Path B (skip-on-drift now) vs Path C (docstring + defer cascade fix to v0.17 MCP milestone); user chose Path C — factory now carries an explicit lossy-import scope note pointing v0.17 as the first-class import milestone. **Notes:** pre-implementation adversarial plan review produced 10 C-findings + 10 F-fixes (C1–C4 + F5 were prerequisite M0 code landed here, C5–C10/F1–F4/F6–F10 shaped the methodology contract folded into the spec); one Codex post-impl review produced the 3 Codex-1/2/3 hardening fixes; one `/adversarial-review` → Codex meta-review loop produced the 3 Meta-C2/3/4 hardening fixes; one final `/advanced-adversarial-review` loop produced the F1 + F3+quarantine fixes above. **Post-triangulation round 2-3 hardening (3 more fixes, same release):** a second `/advanced-adversarial-review` pass converged in Round 3 on three residual blockers missed by the first triangulation. All landed before tag/push. (R2-3-B1, medium) F3 quarantine only suppressed *later* stage callbacks — aggregation (`_getAllIssues` + `_collectHighlights`) still spread `...d.issues`/`...d.highlights` for detectors that had committed partial findings before throwing mid-scan. Promoted `failedDetectors` from a `_runStructuralScans` local to a controller-owned `_lastScanFailedDetectors` field, cleared at the top of each scan, added to on every stage failure (including the legacy `scanTree` loop), and consulted at both aggregation sites to filter their contributions. (R2-3-B2, low-medium) `PerformanceIssue.fromJson.detectedAt` used unguarded `DateTime.parse(json['detectedAt'] as String)` — a malformed ISO string or non-string payload threw and aborted the whole factory, exactly the single-field snapshot-abort pattern Meta-C3 + Codex-2 already hardened the enum fields and `topAllocators` entries against. Replaced with `json['detectedAt'] is String ? DateTime.tryParse(...) : null`. (R2-3-B3, medium) F3 tests were tautological: `_ScopedErrorCapture` did not forward to `_previous?.call(details)` (swallowing TestWidgetsFlutterBinding's layout-error reporting), and per-stage matchers used `expect(matching, isNotEmpty)` (passed if a stage accidentally emitted its error two or three times). Fixed: handler forwards, each stage test asserts `matching.length == 1` AND `nonMatching` is empty, every test that threw a detector error drains it with `tester.takeException()`. Test count: 2,220 → 2,225 (+2 aggregation-filter regression tests + 3 `detectedAt` regression tests; stage-loop tests rewritten in place).
- v0.15.5 — Single-milestone UX patch replacing the overlay's live-reordering "card jumps while I read it" bug with a **freeze-above-on-expand** contract. The user's exact ask: "while I expand an issue card, reordering issue incoming / exist will be happen below index issue that im expanded." v0.15.5 delivers that literally — while any card is expanded, the order of everything from index 0 through the deepest expanded card is frozen against what the user last saw; only the flow beneath that boundary keeps reordering. This supersedes two prior unshipped iterations: (1) "freeze the whole list" (`doc/spec_v0_15_5_freeze_list_order.md`, deleted) — rejected because critical arrivals landed at the bottom past the user's attention and the freeze was invisible with multi-expand; (2) single-card "pin-on-expand" (`doc/spec_v0_15_5_pin_on_expand.md`, deleted) — rejected because only the expanded card itself held its index, so new criticals could still re-rank ABOVE whatever the user was reading. **Changed:** state model in `_FloatingIssuesCardState` is now two coupled fields — `Map<String, int> _expandedIndices` (`issueKey → captured-snapshot-index`) and `List<PerformanceIssue>? _orderSnapshot` (the visible-issues list captured the first time the user expanded any card) — with a class invariant `(orderSnapshot == null) == expandedIndices.isEmpty` asserted at the entry of `applyFreezeZone`. Every mutation path updates both fields in the same `setState`: on 0→1 expand the snapshot is captured *before* writing the first entry; on 1→0 collapse the snapshot is nulled *after* dropping the last entry; `dispose`, `didUpdateWidget` controller-swap, and a `_pruneStaleState` sweep that empties `_expandedIndices` all clear both. The snapshot is sourced from a closure-captured `capturedVisibleIssues` local in the `itemBuilder` body (C1) — NOT from `widget.controller.issuesNotifier.value` at callback time — so a ranker tick between build and tap cannot poison the snapshot with a list the user never saw. **Added:** (1) `applyFreezeZone` top-level `@visibleForTesting` helper — takes `(visibleIssues, orderSnapshot, expandedIndices)` and returns a list whose first `freezeEnd + 1` entries follow `orderSnapshot` using live `PerformanceIssue` instances (so severity/confidence updates still render) and whose remainder is the current ranker flow with frozen keys filtered out. MAX rule: `freezeEnd = max(expandedIndices.values)` — multi-expand freezes the full range `[0..max]` inclusive, including any non-expanded cards *between* two expanded ones. Vanished frozen entries drop silently on the current render (no throw; `_pruneStaleState` evicts the expand entry on its next sweep). Both list-length clamps guard against `freezeEnd > snapshotLen-1` and `freezeEnd > visibleLen-1` so downstream collapses still render. (2) Pin icon (`Icons.push_pin`, 14dp, `theme.textSecondary` at 55% alpha) **repositioned** in `issue_card.dart` from position 4 (between confidence badge and JANK chip) to the last chip slot (after the `↳N` downstream badge, before the Checkbox) — the pin now reads as a "this row is held" marker rather than severity-adjacent chrome. Unconditional `Semantics` wrap preserved: label empty + `excludeSemantics: true` when collapsed so the a11y node identity is stable across expand/collapse. **Fixed:** the reading-a-card reorder bug at its root — `_applyDurationEscalation` flipping warning→critical at 30 cycles, a new CRITICAL arriving mid-read, and any other ranker churn now cannot move anything above the expanded card's index. New criticals enter the flow section beneath the frozen zone. Snapshot coupling bugs — every mutation site now clears both fields together, so a stale `_orderSnapshot` paired with an empty `_expandedIndices` can't survive to the next frame and trip the invariant assert. **Notes:** accepted tradeoff the user explicitly signed off on — a new CRITICAL cannot appear above a card the user is reading until collapse. Multi-expand MAX rule is deliberate; MIN would still let cards above the second expanded card shift, SUM is nonsensical, MAX is the simplest rule that satisfies "nothing above anything I'm reading moves." Pin remains a subtle state hint (55% alpha) — freezing is implicit on expand, so the icon exists to help a user understand why a card that "should have moved" stayed put. `_selectedIssueId` / `_chatIssueStableId` / `_chatHistories` still key against the raw (not visible) set because those surfaces reach ALL issues; narrowing them here would silently hide entries the user can still interact with. **Known pre-existing tradeoff** (NOT a v0.15.5 regression, out of scope): at 300dp overlay width with `title + confidence + JANK + ↳N + pin + Checkbox` all present, the header `Row` overflows horizontally by ~40dp in the Checkbox tail. The pin itself stays within card bounds — F5 regression test asserts this. Tightening Checkbox density is a v0.15.6+ follow-up. Plan: `doc/spec_v0_15_5_freeze_above_on_expand.md`. Test count: 2,170 → 2,194 (+24: 11 pure-function `applyFreezeZone` tests — 9 core + 2 post-impl `freezeEnd=0`/`freezeEnd=length-1` boundary cases — + 5 widget smoke tests in `floating_issues_card_test.dart` + pre-existing `computeVisibleIssues` and `issue_card_test.dart` pin-indicator tests preserved under the renamed groups `v0.15.5 applyFreezeZone` and `v0.15.5 freeze-above-on-expand pin indicator`). **Post-impl adversarial review pass** (4 findings, all applied): SF1 renamed 4 stale `_pinnedIndices` / "pin-on-expand" doc-comment references in `floating_issues_card.dart:396` and `issue_card.dart:93/97/198` to the new `_expandedIndices` / freeze-above vocabulary so future readers aren't misled by contradictions between implementation and narrative; SF2 added the two `freezeEnd=0` / `freezeEnd=length-1` boundary tests; SF3 enriched the `applyFreezeZone` invariant assert message to include `keys=${expandedIndices.keys.toList()}` so a post-mortem from a tripped assert can distinguish a zombie key from a snapshot-only half-state; SF4 documented a known-gap accepted tradeoff — adjacency eviction (a frozen non-expanded neighbour of the expanded card disappearing mid-read due to detector staleness eviction, `computeVisibleIssues` absorbing a standalone under a new root, or downstream collapse) compacts the frozen zone and visibly shifts the expanded card up by one slot; low-frequency (multi-scan eviction while the user is actively reading), self-heals on the next `_pruneStaleState` sweep, and closing it would require a placeholder-row approach (preserving slot geometry for vanished frozen entries at the cost of visible "ghost" cards); deferred beyond v0.15.5. Tactic-12 UI/UX skipped with rationale documented in the plan — 14dp decorative icon relocation within an existing chip row is too narrow a delta to benefit from `/ui-designer` + `/ux-designer` parallel audits over the spec's existing coverage.
- v0.15.4 — Single-milestone patch from `doc/detector_threshold_audit.md` §7 M3: `NetworkMonitorDetector` slow-request threshold tuning + symmetric configurability. v0.15.3's `slowThresholdMs` default of 2000 ms was 2–10× more lenient than any cited 2025–2026 mobile-API source (ideal 100–300 ms, acceptable 500–800 ms, "slow" at ~1 s, "very slow" at ~3 s) and under-fired on exactly the latency regressions the detector exists to catch. Compounding that, the warning tier was constructor-configurable (`slowThresholdMs` optional parameter) but the critical tier was a `static const int _criticalSlowThresholdMs = 5000` — an asymmetry that forced any user who wanted to tune thresholds to fork the detector. **Changed:** three defaults in lockstep — `NetworkMonitorDetector.slowThresholdMs` 2000 → 1000, critical severity boundary 5000 → 3000, `SleuthConfig.slowRequestThresholdMs` 2000 → 1000. **Added:** `NetworkMonitorDetector.criticalSlowThresholdMs` (new constructor parameter, default 3000) promoted from the old hardcoded constant; `SleuthConfig.criticalSlowRequestThresholdMs` (new top-level config field, default 3000) threaded through to the detector at `sleuth_controller.dart:684`. Both constructors carry a debug-mode `assert(criticalSlowThresholdMs > slowThresholdMs, 'critical must be strictly greater than slow so the critical tier is reachable')` — the `SleuthConfig` assert also fires from `copyWith` so `SleuthConfig().copyWith(slowRequestThresholdMs: 5000)` without also raising critical now throws instead of silently producing an unreachable critical tier. Both additions are non-breaking optional parameters with defaults — existing callers continue to compile unchanged, justifying the patch-level version bump (0.15.3 → 0.15.4) despite adding public API surface. `SleuthConfig.minimal()` / `.performance()` presets transparently inherit the new defaults (neither preset sets `slowRequestThresholdMs`). **Tests:** 5 net new. `network_monitor_detector_test.dart` gains `warning just below critical (2999 ms)`, `critical at exactly 3000 ms`, `custom criticalSlowThresholdMs controls severity boundary` (uses two fresh detector instances rather than `clearRecords()` because the latter stamps `_ignoreBeforeTimestamp` at the current fake clock and drops the next default-timestamped record — caught during implementation), and `assert fires when criticalSlowThresholdMs <= slowThresholdMs` (covers both equal and less-than cases). Pre-existing tests hardcoded to old thresholds (`1999` below, `2000` at, `5000` critical) rewritten against the new defaults; the `custom slow threshold works` test gains a `criticalSlowThresholdMs: 1500` override for assert compliance. `config_copy_with_test.dart` gains `criticalSlowRequestThresholdMs must be strictly greater than slow` verifying (a) raising slow above default critical throws, (b) setting them equal throws, (c) raising both in order succeeds. `v2_integration_test.dart` network-config pass-through test extended to assert the new field is threaded through; its existing `slowRequestThresholdMs: 5000` override now carries a matching `criticalSlowRequestThresholdMs: 10000` so the assert stays satisfied. **Migration:** existing code keeps compiling — previously "acceptable" 1.0–1.9 s requests now surface as `slow_request` warnings and previously "warning" 3.0–4.9 s requests now surface as critical; users who considered v0.15.3's values intentional can restore them with `SleuthConfig(slowRequestThresholdMs: 2000, criticalSlowRequestThresholdMs: 5000)` or the equivalent detector-construction override. Test count: 2,166 → 2,170.
- v0.15.3 — Single-milestone patch from `doc/detector_threshold_audit.md` §7 M1: an animation-aware filter on `RepaintDetector`. Closes the asymmetry where `RebuildDetector` already exempts builder widgets via a 3× threshold multiplier but `RepaintDetector` had **zero** filter — any widget at ≥30 paints/sec fired `excessive_repaint`. A `CircularProgressIndicator` spinning at 60 Hz tripped the detector on every session, on every page that mounted it. **Original ship:** `RepaintDetector.animationOwnerNames` const Set with 7 entries (4 Material/Cupertino indicators + 3 generic builder patterns); `_animationOwnerRegex` with `\b…\b` word boundaries; three internal helpers consumed by all three repaint gates. Three-gate filter: **Gate A** (`_evaluateDebugDataPerWidget`) skips per-widget after the rate threshold check; **Gate B** (`_evaluate` wrapper) suppresses the VM aggregate fallback when *every* non-zero entry in `paintCounts` is animation-owned; **Gate C** (`_evaluateDebugData`) computes `residualCount = totalPaintCount - ownedCount`, recomputes `residualRate`, suppresses when residual falls below threshold and surfaces `Excludes N animation-owned paints` suffix when residual still fires. **Post-impl hardening pass (5 findings from `/adversarial-review`, C1–C5):** ownership decisions moved off the chain string entirely and onto a per-paint typed walk that runs at paint-callback time against the live `Element`. **C1 (polymorphic-key collision):** coordinator cached chain on first occurrence per `typeName` key, so two `CustomPaint` widgets sharing the key — one inside `AnimatedBuilder`, one driven by external `setState` — were either both fully suppressed or both fully fired. Fix: per-paint owned attribution at `_handleProfilePaint` calls `isAnimationOwnedPaint(element, chain)` against the live `Element`, exposing results via two new `DebugSnapshot` fields `animationOwnedPaintCounts` (Map<String, int>) and `totalAnimationOwnedPaintCount` (int) that the detector now reads instead of re-deriving ownership from the chain. **C2 (insufficient owner set):** original 7 entries missed the `Animated*` family (12 widgets: `AnimatedContainer`, `AnimatedRotation`, `AnimatedScale`, `AnimatedSize`, `AnimatedOpacity`, `AnimatedAlign`, `AnimatedPadding`, `AnimatedPositioned`, `AnimatedPositionedDirectional`, `AnimatedSlide`, `AnimatedSwitcher`, `AnimatedCrossFade`, `AnimatedFractionallySizedBox`) plus `Hero` and `RefreshIndicator`; expanded to 21 entries and moved canonical Set out of `RepaintDetector` into shared `lib/src/utils/animation_owner_names.dart` so coordinator and detector reference the same source of truth. **C3 (chain-walks-up gap):** when a `CircularProgressIndicator` is mounted without a wrapping `RepaintBoundary`, the dirty mark propagates UP to the nearest layer-owning ancestor (often `Center` or `Stack`) and the captured chain walks UP from the ancestor — `CircularProgressIndicator` is a *descendant* of the leaf and chain-containment misses it. Fix: `isAnimationOwnedPaint` adds a **bounded-depth descendant walk** (`hasAnimationOwnerDescendant`, `maxVisits=32`, `maxDepth=4`) over the leaf's children with typed runtimeType match. **C4 (chain capture exception safety):** `element.visitAncestorElements` can throw "Looking up a deactivated widget's ancestor is unsafe" mid-tear-down; pre-fix, the exception unwound through `_handleProfilePaint` and crashed the entire instrumentation pipeline. Fix: wrapped chain capture in try/catch at `debug_instrumentation_coordinator.dart` and log a one-line warning instead of propagating; detected paint counting continues, only the chain enrichment is skipped for that single event. **C5 (test fixture tautology):** original v0.15.3 only had a real-widget test for `CircularProgressIndicator`; the other 6 owners + C1/C3 cases were covered only by hand-rolled fixtures that mirrored the filter's own assumptions. Fix: new `test/detectors/repaint_animation_owners_real_widget_test.dart` adds 8 real-widget tests (LinearProgressIndicator, RefreshProgressIndicator, TweenAnimationBuilder, AnimatedBuilder, ValueListenableBuilder, AnimatedContainer, C1 mixed-ownership scene, C3 bare-CPI-without-RepaintBoundary scene). The TweenAnimationBuilder and ValueListenableBuilder tests immediately caught a real generic-stripping bug where `hasAnimationOwnerDescendant` was looking up `'TweenAnimationBuilder<double>'` against a Set containing `'TweenAnimationBuilder'` — exactly the class of bug the anti-tautology tests exist to catch (adversarial-investigation Tactic 9). **Architectural follow-ups discovered during C5:** generic-stripping fix in `hasAnimationOwnerDescendant` (single `indexOf('<')` slice before Set lookup); new `hasAnimationOwnerAncestor` walk (`maxDepth=16`) because `RefreshProgressIndicator` exposed a third gap — its painted leaf (`CustomPaint`) sits ~13 ancestors below the wrapping `AnimatedBuilder` owner due to Material's `_buildMaterialIndicator` decoration stack (`Padding > SizedBox > _SemanticsWrapper > NotificationListener > Material > Padding > Opacity > Transform > CustomPaint`). The chain string fails because `buildAncestorChain`'s `maxDepth: 6` is deliberately too shallow for human-readable display; the descendant walk fails because the owner is *upstream*. `isAnimationOwnedPaint` now has three legs, checked cheapest-first: (1) chain-string regex, (2) typed ancestor walk independent of chain budget, (3) typed descendant walk. **KDD-2 (chain-containment first, deeper typed walks fall through):** the chain regex stays as the cheapest first-leg check; the typed ancestor and descendant walks only run when the chain misses. **KDD-3 (residual subtraction at Gate C):** at the aggregate path we know `totalPaintCount` but lack per-widget attribution for everything that isn't in `paintCounts`; subtraction lets us still fire if a surrounding scene is genuinely doing too much paint work. **KDD-4 (asymmetric vs RebuildDetector is deliberate):** full exemption (Repaint) vs 3× multiplier (Rebuild) — `CircularProgressIndicator` is *supposed* to paint at the device refresh rate; rebuild rate on the same widget is more ambiguous (could be a parent re-mounting it 60×/sec by mistake). **KDD-6 superseded by C1 fix:** polymorphic-key collision is no longer a known limitation — per-paint attribution handles mixed ownership for the same `typeName` correctly. Plan: `doc/spec_v0_15_3_repaint_animation_filter.md`. Test count: 2,146 → 2,166 (+10 hand-rolled gate-algebra tests + 1 real-widget CPI anti-tautology test + 8 C5 real-widget owner-coverage tests + 1 gate-algebra extension for the new owned-counts contract).
- v0.15.2 — UX refactor of the rebuild-stats surface, prompted by another real-device profile review. The v0.15.1 design surfaced rebuild attribution two ways: a `rebuild_hotspot_summary` IssueCard in the warning stream (≥20 builds/sec gate) and a small always-on chip above the issue list that linked into the drilldown. The IssueCard collided with the issue ranker, fired only on hot routes, and KDD-5 inflations turned route entry into a noisy warning storm; the chip was discoverable but offered no signal about *what* was rebuilding. v0.15.2 collapses both surfaces into a single always-on inline expandable panel. Changed: `_RebuildStatsBanner` is now an expandable inline panel — collapsed shows `Rebuilds: N across M widgets` + chevron, expanded reveals the **top-3** widget rows with rank, name, live-tweened count, normalised bar fill, a Pause/Resume freeze toggle, an `incl. inflations` footnote, and a `See all N →` link that pushes the unchanged `RebuildStatsPage` drilldown via the same snapshot-and-push path the rollup IssueCard used to use. Reactivity is via `Listenable.merge([issuesNotifier, routeHistoryNotifier])` — the panel rebuilds on every scan tick and on every route push/pop, and pause auto-clears on route change. Removed: the entire `rebuild_hotspot_summary` rollup machinery — `RebuildDetector._maybeEmitRollupIssue`/`_sessionElapsed`/`_formatElapsed` helpers, `_activeRouteSession` field, all four rate/duration constants, `BaseDetector.updateActiveRouteSession` hook (no remaining overrides) and the matching controller call site, `IssueCard.onSeeAllRebuilds` parameter + `_buildSeeAllRebuildsLink` helper + conditional render block, `FixHintBuilder.rebuildHotspotSummary`, the encyclopedia entry + four `relatedIssues` cross-references (`rebuild_activity`, `rebuild_debug`, `setstate_scope`, `animated_builder_no_child`), and the `'rebuild_hotspot_summary'` entry from the controller's stableId prefix map. Added: `TweenAnimationBuilder` to `_frameworkWidgetDenyList` (48 → 49 framework widgets) — the new panel uses it for the live count tween, and the KDD-10 audit test caught it as a missing entry. Without this addition, Sleuth would re-introduce a self-measurement leak on its own panel. 5 new banner tests in `floating_issues_card_test.dart`: collapsed-by-default, expand reveals top-3 + footer + correct chevron and pins the top-3 contract (4th widget is NOT inlined), expand → tap "See all N →" → drilldown push, and pause freezes rendered counts across subsequent live merges. Fixed: profile-mode KDD-5 inflations no longer pollute the warning stream — because the rollup IssueCard is gone, route entry no longer surfaces a critical-severity card for transient inflations that decay as the tree stabilises. The data is still visible in the panel, but as data, not a warning. Test count: 2,153 → 2,146. **Post-impl hardening pass** (12 findings from `/adversarial-review`): C1 paused-snapshot drift — `_RebuildStatsBanner.onTap` now takes `Map<String, int>? overrideCounts`; footer GestureDetector passes `_paused ? _frozenCounts : null` so the drilldown opens with what was on screen, not what the live session has drifted to. C2 redundant "See all N" on small routes — footer link now gated on `widgetCount > _topN`. C3 stale test docstrings rewritten for the panel-only contract (no more `_rollupMinTotalForRate = 30` references). H1 tap targets — pause toggle wrapped in 28×28 SizedBox, "See all" in 24-tall SizedBox, both `HitTestBehavior.opaque + Center(widthFactor: 1)` so inner GestureDetector wins against outer header (compromise vs Material 48dp ideal — documented in code, ~330dp test budget vs ~446dp phone). H2 silent auto-resume — `_RebuildStatsBannerState` fires `onPauseDiscarded` callback when route change clears pause; parent shows 2 s "Pause cleared — route changed" snackbar. H3 KDD-5 disclosure regression — initial fix added an `Icons.info_outline` (12dp) glyph next to the collapsed count, reverted on user feedback because the non-interactive icon visually competed with the existing `Icons.repeat` rebuild glyph already in the same row; caveat now lives only in the expanded `incl. inflations` footnote and on `RebuildStatsPage` (accepted tradeoff vs visual noise on the always-visible header). H4 empty-state debuggability covered by existing `_rebuildSessionGoneVisible` snackbar (verified through C1 callback signature change). F1 collapsed pause indicator — `Icons.pause` (10dp, alpha 0.5) renders next to count when `_paused`. F2 `TweenAnimationBuilder<int>` smell audited and confirmed canonical (`begin: 0` is the seed-only value; framework substitutes current animated value as new begin on rebuild — no code change). F3/P3 hoisted `Listenable.merge` out of `build()` into `late final _mergedListenable` initialised in `initState`. TF2 drilldown contract test extended: pause → mutate counts → tap "See all 4 →" → assert drilldown shows frozen 15/×8, NOT live 20/×13, using `find.descendant(of: RebuildStatsPage, ...)` to disambiguate panel vs drilldown text matches when both are mounted.
- v0.15.1 — Self-measurement + time-blind-threshold fix. Real-device profile run on a home screen where DevTools reported ~50–100 rebuilds showed v0.15.0 claiming 21,352 rebuilds, with the drilldown dominated by `FloatingIssuesCard`, `IssueCard`, `TriggerButton`, `Container`, `Padding`, `ValueListenableBuilder`, `FadeTransition` — Sleuth measuring itself. Root cause (KDD-10): Flutter's `_isLocalCreationLocationImpl` fallback at `widget_inspector.dart:1801-1816` returns `!file.contains('packages/flutter/')` when `_pubRootDirectories == null`, so `package:sleuth/...` is classified as "user widget" and emitted through `FlutterTimeline.startSync`. `WidgetInspectorService.addPubRootDirectories` is `@protected` and additive-only, so exclusion at the framework layer is unavailable. Fix: `_frameworkWidgetDenyList` inside `DebugInstrumentationCoordinator` — a const `Set<String>` of 48 framework widgets used in `lib/src/ui/` + 25 Sleuth overlay widget classes, checked inside `canonicalizeTypeName` AFTER generic stripping so `ValueListenableBuilder<int>` collapses to `ValueListenableBuilder` before the set lookup. Filter is now five layers: `_denyList → isRenderObjectName → identifierRegex → genericStrip → frameworkWidgetDenyList`. CI audit test `test/debug/overlay_denylist_audit_test.dart` walks `lib/src/ui/**/*.dart` with `dart:io` and enforces three invariants: every class extending `Stateless/Stateful/InheritedWidget` is in the denylist, every framework widget from a curated candidate set actually used in overlay code is in the denylist, no stale framework entries remain. `@visibleForTesting static debugFrameworkWidgetDenyList` exposes the set. Parameterized denylist unit tests (in `debug_instrumentation_coordinator_profile_test.dart`) assert every entry returns null from `canonicalizeTypeName` + generic collapse + look-alike user widgets (`ContainerPro`, `MyText`, `_MyPrivateCard`) still pass through. KDD-9: absolute-total rollup threshold (v0.15.0: `> 100` warning, `> 300` critical) replaced with sustained-rate: `≥ 20 builds/sec` warning, `≥ 50 builds/sec` critical, gated by 30-build + 1.5-second noise floor. Rate computed via injected `DateTime Function() clock` on `RebuildDetector` so tests are deterministic. Title format: `"Build Hotspot: 22.0 builds/sec (110 across 4 widgets in 5.0s)"`. Disclaimer copy updated in three places (rebuild_detector detail, issue_explanation_builder encyclopedia entry, rebuild_stats_page inline banner) with KDD-10 Sleuth-overlay exclusion note. Test count: 2,140 → 2,153.
- v0.15.0 — Profile-mode per-widget rebuild counting via `FlutterTimeline.debugCollect()` attributed to the active `RouteSession`. New: `DebugSnapshot.source` (`RebuildCountSource` enum: `none`/`debugCallback`/`flutterTimeline`) tags snapshot provenance; `DebugInstrumentationCoordinator.installProfileMode()`/`uninstallProfileMode()` flip `FlutterTimeline.debugCollectionEnabled` with install-time refusal on flag-already-true (DevTools conflict, KDD-1 + R20); `canonicalizeTypeName` three-layer filter (emission gate → generics strip → deny-list + identifier regex) drops framework frame scopes (`BUILD`/`LAYOUT`/`PAINT`/`FINALIZE TREE`) and collapses `Provider<Foo>`→`Provider`; `RouteSession.rebuildCountsByType` + `totalRebuilds` additively accumulate per-scan with JSON round-trip; new `rebuild_hotspot_summary` rollup issue emitted by `RebuildDetector` when `source==flutterTimeline` && `session!=null` && `session.totalRebuilds>100` (per-type issues still fire alongside — rollup is a summary card, not a replacement); new `RebuildStatsPage` drilldown page with snapshot-at-open semantics (copies counts at construction, never live-updates) reachable via "See all rebuilds →" link on rollup `IssueCard` (`onSeeAllRebuilds` callback, caller-gated); `primeExistingElements()` coordinator helper lets test helpers count the very first rebuild after install; encyclopedia entry for `rebuild_hotspot_summary` with bidirectional `relatedIssues` links (`excessive_rebuilds`, `setstate_scope`, `animated_builder_unscoped`). Changed: 4 assert-wrapper sites restructured to explicit `if (kDebugMode) {} else if (!kReleaseMode && config.enableDeepDebugInstrumentation) {}` mode splits (M3) — previously the entire rebuild-attribution pipeline was a silent no-op in profile builds because `assert(() {})` stripped; drain→attribute→route-switch ordering in `_scanTreeInner` (counts always land on the pre-route-change session, M7); `_scanInProgress` re-entry guard (M5); coordinator construction gate widened to include `enableDeepDebugInstrumentation` alone (KDD-8). Fixed: profile-mode rebuild attribution silent no-op (v1 review C1/C4, first release that actually collects data in profile mode), `FlutterTimeline.debugCollectionEnabled` conflict path (R20), test pollution via static `FlutterTimeline._buffer` (C6 — all profile tests now save/restore flag in setUp/tearDown). One adversarial plan-review round before implementation plus one post-implementation adversarial code review. **KDD-5 semantic gap:** profile-mode counts include widget inflations as well as `setState`-driven rebuilds (the Flutter framework emits the same `BUILD` timeline scope for both), so route entry shows transient elevated counts that decay as the tree stabilises — disclaimer surfaces inline on rollup issue detail and on the drilldown page. **R3 test limitation:** widget tests run under `kDebugMode == true`, so M12 controller and coordinator tests inject a fake coordinator tagged `RebuildCountSource.flutterTimeline` to exercise the merge pipeline end-to-end without profile-mode compilation; full validation is the M1 probe under `fvm flutter run --profile` against a physical device. Test count: 2,092 → 2,140.
- v0.14.1: Per-tab `RouteSession` tracking for bottom-nav / tab-shell apps (`IndexedStack`, `StatefulShellRoute.indexedStack`, `CupertinoTabScaffold`). New: `RouteSession.scaffoldHashKey` (identityHashCode of innermost visible Scaffold Element) forms a compound `(routeName, scaffoldHashKey)` session key so each tab owns a distinct session; `RouteSession.tabVisitIndex` 1-indexed ordinal for repeat visits; `RouteSession.hotReloadGeneration` debug-only counter; `PerformanceIssue.scaffoldHashKey` + `PerformanceIssue.tabVisitIndex` stamped at aggregation time (routeName kept RAW — no `(tab-N)` suffix baked in); `PerformanceIssue.routeDisplayName` getter for human-facing labels (IssueCard, AI chat context, issue explanation substitution all route through it); `TabBarView` / `PageView` boundary filter in the scan visitor so inline sub-tab swipes stay inside the outer route's session. Changed: `SleuthConfig.routeHistoryCapacity` default raised 20 → 50; markdown exporter renders tab suffix from `tabVisitIndex`. One adversarial plan-review round before implementation + one post-impl adversarial code review. Fixes: C1 `tabVisitIndex` collision after FIFO eviction (`max+1` not `count+1`), C2 hot reload did not close active session (`_reassembleInternal()` now closes before bumping generation), C3 `PerformanceIssue.routeName` overwrite loses raw name (raw preserved, display via getter), E1 type-unsafe `fromJson` (`is int` guards on per-tab fields). Test count: 2,051 → 2,092 (+15 feature + 16 #16 serialization + 10 post-review regression).
- v0.14.0: Route Scoping (data + export, no overlay UI). New: `RouteSession` model with composite `healthScore` (FPS 40pts + jank 30pts + issues 30pts, normalised to `fpsTarget`), passive route detection via element tree walk (no NavigatorObserver), `routeIgnorePatterns` config (exact + wildcard), `routeHistoryCapacity` config, `Sleuth.routeHistory` static API, `Sleuth.routeHealthScore()` static API, schema v4 with `routeSessions` export field, "Route Health" markdown table with health dots/duration. `SleuthConfig.copyWith()` extended with 2 new fields. Route filter bar and summary row removed from overlay after on-device review (historical issues not surfaceable as cards, making filter UX misleading; data model + export retained for programmatic use). Adversarial review fixes: unnamed route session leak (counter incremented before comparison), health score hardcoded 60 FPS denominator (now uses `fpsTarget`), `packageVersion` stale ('0.12.1'→'0.14.0'), `fpsPercentiles()` triple-call in `toJson()` (cached), per-route export FPS clamped to `fpsTarget`. Test count: 1,988 → 2,050.
- v0.13.1: Dark/light mode toggle, design system tokens, Icons.pets brand icon, header optimization, false-positive fix. New: in-overlay theme toggle icon, `Sleuth.updateTheme()` static API, `SleuthController.themeOverride` ValueListenable, `didChangePlatformBrightness` system reactivity (gated to auto-detect mode), 9 typography tokens (`fontXxs`–`fontDisplay`) and 7 border radius tokens (`radiusSm`–`radiusFull`) on `SleuthThemeData` with full migration across all 8 overlay UI files (~119 font sizes, ~50 border radii), `triggerIconColor` theme token. Changed: brand icon migrated from dog emoji/CustomPainter to `Icons.pets` (Material paw print) across trigger button, overlay header, guide page, and example app. `sleuth_logo_painter.dart` deleted. Startup metrics page values right-aligned. Guide icon moved from header to footer, highlight toggle shrunk to 24px, footer Semantics consistency, `gripDots` light theme override, color coupling documentation. Fixed: `stateful_density` false positive from Sleuth overlay widgets (8 names added to `_frameworkWidgetNames` exclusion set), hardcoded spacing values replaced with design tokens, GlobalKey demo unreliable detection (itemExtent 40→24dp, itemCount 30→40 to account for DemoScaffold chrome and trailing-only cache at scroll 0). Five adversarial reviews (general ×2, theme performance + design system, icon migration, GlobalKey demo), all findings fixed. Test count: 1,985 → 1,988.
- v0.13.0: Startup Performance Tracing: `Sleuth.init()` + `Sleuth.markInteractive()` for TTFF/TTI measurement, new `StartupDetector` (23rd detector, structural lifecycle, one-shot), `slow_startup_ttff` issue type, `StartupMetrics` model with per-phase FrameTiming breakdown and VM sub-phase enrichment slots, `IssueCategory.startup` across all UI/export surfaces, startup metrics banner in overlay, encyclopedia entry (47 total), markdown export section. Three adversarial reviews + full 23-detector accuracy audit. Fixes: clock-domain mismatch (monotonic→wall-clock TTFF), stableId prefix mapping, causal graph layout_bottleneck visibility (8 incorrect downstream rules removed, 52→44 rules), ShaderJankDetector Impeller notice removed (noisy false positive), stateful_density framework widget noise, shallow_rebuild_risk ScrollNotificationObserver suppression, NetworkMonitorDetector route-based clearing (replaces time-based staleness eviction). Test count: 1,915 → 1,985.
- v0.12.2: Post-Codex adversarial review hardening: timeline pipeline exception isolation (`_onTimelineData` try/finally + per-detector try/catch), encyclopedia placeholder leak fix (substitute all entries with fallback sentinel), cookbook slow-frame detector staleness fix (time-based eviction via `_TimestampedFrame`). One adversarial review, 3 findings fixed. See `CHANGELOG.md`.
- v0.12.1: Pillar 6 Part 2: Overlay UI, Diagnostics Output & Export. New: trigger button alignment config (`triggerButtonAlignment`/`triggerButtonOffset`), minimize/maximize/restore card controls (3-state window), recurrence badge on IssueCard, context-aware encyclopedia entries (`IssueExplanationBuilder.substitute()`), inline confidence reasoning, dismissible debug-mode banner, `Sleuth.exportSummary()` markdown export, "Copy conversation" button on AiChatPage. Adversarial review fixes: Tooltip→Semantics in overlay (OverlayPortal crash), GFM escaping in copy/export, recurrence badge overflow, semantic labels on interactive elements, cookbook detector false-positive filter. See `doc/spec_v11.md`.
- v0.12.0: Pillar 6 Part 1: Public API & Authoring Surface + real-device VM connection fix. **Breaking**: `SleuthConfig.treeScanInterval` now takes `Duration` instead of `int` ms. New: `SleuthConfig.minimal()`/`.performance()` presets, threshold doc comments on every parameter, debug-mode assert validation (14 rules), `SimpleStructuralDetector` helper base class, custom detector key gating (`disabledCustomDetectorKeys`), three-file custom detector cookbook in example app. VM fix: `controlWebServer` replaces `getInfo()` for cold-start port bind, background reconnect ladder (500ms→30s), `_connectInFlight` concurrency guard, frameStatsNotifier 5Hz throttle (prevents self-feedback rebuild loop), per-detector exception isolation in unified walk, post-dispose continuation guards. One adversarial review, 4 findings fixed (exception isolation, post-dispose guards, diag print removal, throttle test coverage). Test count: 1,825 → 1,869.
- Test count: 1,869 → 1,915 (root) + 7 → 9 (example).
- v0.11.1: Pillar 5 Part 2 demo quality enhancements: Before/After toggle in `DemoScaffold`, working fixed-pattern bodies for all 23 demos, live `MetricsBar`/`MetricChip` in 7 demos, two new combined demos (E-Commerce, Chat). Three adversarial review rounds, 18 findings + KeepAliveDetector bug fix. See `doc/spec_v11.md`.
- v0.11.0: Pillar 5 Part 1 demo infrastructure: DemoScaffold shared layout, 5 new demos (shader jank, platform channel traffic, memory pressure, GPU pressure, missing RepaintBoundary), categorized home screen (8 categories, 23 demos). Two adversarial reviews: (round 1) memory MB overcount fix, theme-aware color fix; (round 2) critical GC rate dilution fix (10s sliding window replaces lifetime-based denominator in `MemoryPressureDetector`), setState-after-dispose guards + global `debugProfilePlatformChannels` save/restore in platform channel demo, memory label clarification, RepaintBoundary description accuracy, Impeller warning banner on shader jank page. See `doc/spec_v11.md`.
- v0.10.9: Pillar 4 issue documentation quality: 8 missing encyclopedia entries (38→46 total), readingTheData for all 46 entries, build/memory/paint/raster/network content enrichment (14 entries), relatedIssues cross-references with bidirectional links derived from causal graph, related issues UI chips with scroll-to, AI context integration, content quality guardrail tests. Adversarial review: chip scroll-to bug fix (per-entry GlobalKey map), repaint_debug bidirectional relations, content threshold enrichments. See `doc/spec_v11.md`.
- v0.10.8: Pillar 3b output & presentation: confidenceReason field on all 22 detectors, severity auto-escalation (warning→critical after 30 cycles), 2 new correlator rules (structural+jank, structural+rebuild), multi-ancestor source locations, packageName extraction, session summary export (topIssues/causalEdges/frameHistogram/detectorHitRates/memoryTrendSummary), schema v3. Adversarial reviews: 3 missing stableId prefix mappings (Pillar 3b); scan chain exception safety, stable duplicate-request IDs, POST duplicate exclusion, GlobalKey route-change guard, sliver correlator coverage (full branch). See `doc/spec_v11.md`.
- v0.10.7: Pillar 3a enrichment: Expanded causal rules (37→52), RecurrenceTrend ring-buffer time-series, InteractionContext typing+appLifecycle, WidgetHeatMapEntry aggregation, FixBaseline/FixVerificationResult with 5-cycle cooldown. Adversarial review: framework filter expansion, grace period counter reset. See `doc/spec_v11.md`.
- v0.10.6: Pillar 2b resource management: Adaptive scan frequency (M4), _getAllIssues generation cache (M5), detector lazy initialization (M6), debug callback TypeNameCache (M7). Adversarial review: timer leak fix, parallel timer chain guard, concurrent detector mutation guard. See `doc/spec_v11.md`.
- v0.10.5: Pillar 2a hot-path performance: TypeNameCache (M1), highlight dirty-check (M2), timeline case-matching (M3). Reduces tree walk allocations by ~40-60%.
- v0.10.4: v11.19: Sliver anti-pattern detection in ListviewDetector (SliverToBoxAdapter large subtrees, SliverFillRemaining misuse, shrinkWrap inside slivers). See `doc/spec_v11.md`.
- v0.10.3: v11 detector audit Part 3 (6 milestones: duplicate request detection, GlobalKey recreation, subtree cost enrichment, thread attribution, warmup/builder suppression). See `doc/spec_v11.md`.
- v0.10.0: Issue Encyclopedia, contextual AI Chat, IssueCard shimmer Ask AI link, responsive action links, MediaQuery optimization, overlay UI polish. See CHANGELOG.md.
- v0.9.2: v9 accuracy/self-overhead/correctness: 17 milestones — opacity value semantics, paint phase classification, inner subtree walk elimination, highlight overlay GPU optimization, generation-counter shouldRepaint, and more.
- v0.9.1: v8 accuracy/correctness/performance fixes: SetState O(N²) fix (v8.1), scaffold scan-root fallback for Cupertino/scaffold-free apps (v8.2), HTTP monitor leak (v8.3), platform channel false positives (v8.4), SetState wording (v8.5).
- v0.9.0: Unified structural tree walk (v7.9), all v7 milestones shipped.
- v0.8.2: Ring buffers (v7.7), correlator sort cache (v7.8), VM reconnect polling fix (v7.10).
- v0.8.1: Accuracy fixes — HeavyCompute two-tier (v7.1), NetworkMonitor threshold (v7.2), threshold tuning (v7.3), correlator coverage (v7.4), rebuild VM fallback (v7.5), MemoryPressure warmup (v7.6).
- v0.8.0: v6 roadmap complete (22 milestones: safety, accuracy, UI polish, testing).
- v0.7.0: v4 + v5 roadmap — suppression, custom detectors, theming, export enrichment, causal graph, configurable thresholds, detector registry, network correlation, RepaintBoundary detector.
- v7 roadmap: 10/10 milestones shipped ✅. See `doc/spec_v7.md`.
- v8 roadmap: 5/5 milestones shipped ✅ (v8.1–v8.5). See `doc/spec_v8.md`.
- v10 roadmap: 12/12 milestones shipped ✅ (v10.1–v10.12). See `doc/spec_v10.md`.
- v9 roadmap: 17/17 milestones shipped ✅ (v9.1–v9.17). See `doc/spec_v9.md`.
- Post-implementation audits in `doc/spec_v2_audit.md` and `doc/spec_post_impl_notes.md`.
- Full spec index in `doc/implementation_spec.md`.
- v11 roadmap: 19/19 milestones shipped ✅ (v11.1–v11.19, four adversarial reviews). See `doc/spec_v11.md`.
- v11 Pillar 4: 10/10 milestones shipped ✅ (v11.20–v11.29, one adversarial review). See `doc/spec_v11.md`.
- v11 Pillar 5 Part 1: 7/7 milestones shipped ✅ (M1–M7, two adversarial reviews). See `doc/spec_v11.md`.
- v11 Pillar 5 Part 2: 7/7 milestones shipped ✅ (M8–M14, three adversarial review rounds + KeepAliveDetector bug fix). See `doc/spec_v11.md`.
- v11 Pillar 6 Part 1: 7/7 milestones shipped ✅ (M1–M7, one adversarial review). See `doc/spec_v11.md`.
- v11 Pillar 6 Part 2: 8/8 milestones shipped ✅ (M1–M8, one adversarial review round + 6 fixes). See `doc/spec_v11.md`.
- 2,417 tests (root) + 9 tests (example), 0 analysis issues
