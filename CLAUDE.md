# Sleuth

Runtime performance diagnostics package for Flutter mobile apps. 23 detectors across 4 lifecycle types (runtime, vmOnly, hybrid, structural).

## Commands

```bash
# Always use fvm for all Flutter/Dart commands
fvm flutter test                    # Run all tests (~2,634 tests, ~30s)
fvm flutter test test/detectors/    # Run detector tests only
fvm flutter analyze                 # Static analysis (must be 0 issues)
fvm flutter pub publish --dry-run   # Verify publish readiness

# Example app
cd example && fvm flutter run --profile   # Profile mode (recommended)
cd example && fvm flutter run             # Debug mode
cd example && fvm flutter test            # Cookbook smoke tests (9 tests)
```

## Architecture

```
lib/
  sleuth.dart          # Public API barrel file + Sleuth entry point
  src/
    models/                     # Data classes: PerformanceIssue, FrameStats, FrameVerdict, BaseDetector
    detectors/                  # 23 detector implementations (one file per detector)
    network/                    # HTTP monitoring: SleuthHttpOverrides, RequestRecord
    analyzer/                   # RenderPipelineAnalyzer, FrameEventCorrelator
    controller/                 # SleuthController (orchestrates detectors, config, scan loop)
    vm/                         # VmServiceClient, TimelineParser
    debug/                      # DebugInstrumentationCoordinator, DebugSnapshot
    ranking/                    # IssueRanker (weighted composite scoring)
    ui/                         # Overlay widgets: FloatingIssuesCard, IssueCard, AiChatPage, IssueEncyclopediaPage, TriggerButton, SleuthTheme
    utils/                      # WidgetLocation helper, FixHintBuilder, TypeNameCache
    validation/                 # EvidenceTier, DetectorMetadata, ComponentMetadata, ProfileCaptureSchema
test/
    mirrors lib/src/ structure + helpers/ and benchmark/
    validation/                 # Per-detector reproducers, audit invariants, capture schema tests
```

### Key patterns

- **Detectors** extend `BaseDetector` (in `models/base_detector.dart`). Each has a `DetectorType` enum value and `DetectorLifecycle` (runtime, vmOnly, hybrid, structural). Built-in detectors implement 4 lifecycle methods (`prepareScan`, `checkElement`, `afterElement`, `finalizeScan`) for the unified tree walk. Custom detectors override `scanTree` directly.
- **SleuthController** owns all detectors, runs the scan loop (unified single-pass tree walk for all 16 tree-scanning detectors), and manages the `FrameVerdict` pipeline.
- **Three-tier verdict**: Correlated (VM timeline matched per-frame) > Full (VM batch) > Basic (FrameTiming only). Falls back automatically.
- **Evidence tier ledger**: every built-in detector carries `DetectorMetadata` declaring its `EvidenceTier` (`unvalidated` / `reproducerOnly` / `runtimeVerified` / `externallyCited`). Raises require a hermetic reproducer + audit-gate entries. Ledger: `doc/validation_ledger.md`.
- **Reproducer path convention**: `reproducerPath` points at `test/validation/<d>_reproducer_test.dart` (purpose-written, v0.16.3 / v0.17.1) or `test/detectors/<d>_detector_test.dart` (reused unit tests, v0.17.2). Both pass `isPathInsideRepo`. Pick whichever fits the detector's test shape.
- **Family declaration**: `coveredStableIds` for bare/colon-parametric families (`excessive_keep_alive:<i>`); `parametricFamilies` (since v0.17.3) for underscore-parametric (`repaint_debug_<typeName>`). Matcher requires non-empty suffix after `_`.
- Test helpers live in `test/helpers/` — `benchmark_helpers.dart` and `timeline_test_helpers.dart`.

## Conventions

- New detectors: add enum value to `DetectorType`, create detector file in `detectors/`, implement `prepareScan`/`checkElement`/`afterElement`/`finalizeScan` (not `scanTree`), register in `SleuthController`, add tests mirroring existing detector test structure. Tests call `scanTree` (the base class wrapper) — no special test setup needed.
- Issues use `IssueConfidence`: confirmed (directly observed), likely (runtime + structural), possible (structural only).
- Fix hints use `FixHintBuilder` (in `utils/fix_hint_builder.dart`) — never hardcode fixHint strings in detectors.
- All public API goes through `lib/sleuth.dart` barrel file.
- Package is completely disabled in release mode (`kReleaseMode` guard).

## Current state

**v0.19.5** (current) — Parser-hardening release. No tier raises. Distribution unchanged from v0.19.4: **20/23 reproducerOnly base, 3/23 effective runtimeVerified families** (HeavyCompute.heavy_compute base + MemoryPressure.heap_growing per-family + PlatformChannel.platform_channel_traffic base). `TimelineParser` B/E reconstruction extended from BUILD-only (v0.18.2) to LAYOUT, PAINT, and raster events via shared `_reconstructPhaseBE` helper with stack-drain-empty outermost-only emission — iOS Impeller emits these phases as nested B/E pairs (`LAYOUT (root)` wraps `LAYOUT`; raster trio `GPURasterizer::Draw` wraps `Rasterizer::DoDraw` wraps `Rasterizer::DrawToSurfaces`); pre-v0.19.5 they were silently dropped, leaving `_lastRasterUs`/`_lastUiUs` zero on Impeller captures. New `_pendingPhaseBeginsCapPerTid = 100` cap mirrors BUILD's; `VmServiceClient` carries 3 new pending-begins maps threaded through `parse()`, cleared in `_cleanup()`, swept by shared `_evictStaleBegins`. On-device verification (iPhone 12 / iOS 17.5 / Flutter 3.41.x): 155 frames × 2.12 ms/frame outermost-raster `dur`. New public API `Sleuth.diagnoseCaptureState()` returns `(initialized, captureMode, vmConnected)` for capture-screen tooling. A GpuPressure `raster_dominance` runtimeVerified raise was investigated and not shipped — see `doc/validation_ledger.md` for the three structural blockers (ratio axis unforceable on iOS profile mode given UI/raster cost asymmetry; `_crossCheckTraceVsObserved` skips for non-time units so `'ratio'` bracket has no independent schema witness; `processTimelineData` splices raster/UI across polls). Future raster-related raise needs absolute-ms axis + single-poll-snapshot detector evaluation. 2,844 tests passing (+9 net from v0.19.4). `fvm flutter analyze` clean.

**v0.19.4** — `PlatformChannelDetector.platform_channel_traffic` raised to **runtimeVerified** (base-tier raise, single-family detector — HeavyCompute pattern). Distribution: **20/23 reproducerOnly base, 3/23 effective runtimeVerified families** (HeavyCompute.heavy_compute base + MemoryPressure.heap_growing per-family + PlatformChannel.platform_channel_traffic base). Frequency axis only — the 8 ms cumulative-duration axis and 41 calls/sec critical tier remain implicitly reproducerOnly. No BREAKING, no public API changes. **Detector behavior change (live monitoring)**: cooldown semantics now suppress fresh emissions during the 3-cycle drain after a fire (retains the prior `_lastEmittedIssue` with original `dedupIdentityMicros`) so sustained overload produces ONE trace record per cooldown window instead of one per evaluation cycle. Symmetric severity-mismatch carve-out: warning↔critical severity changes during cooldown emit a fresh issue with new identity so live monitoring surfaces both escalations and de-escalations in real time. `SleuthController.resetCaptureState()` now also resets PlatformChannelDetector state so back-to-back capture legs aren't suppressed by leftover cooldown from a prior leg. Detector exports `_recentCallCount` + `_cumulativeDurationUs` into trace event args via new `PerformanceIssue.extraTraceArgs` field; audit gate cross-checks send-side `magnitudeObserved` against detector-observed `args["observedCount"]` within ±25% so mislabeled-leg captures fail audit. In-screen validator mirrors `bracketRequireUniqueDetectedAtMicros` invariant before allowing export. Three on-device captures (iPhone 12 / iOS 17.5 / Flutter 3.41.x) at below (15 calls/sec sub-threshold), at (25 calls/sec), above (35 calls/sec) bracket bands recorded via the new `PlatformChannelCaptureScreen`. `bracketAtTolerance: 0.50` → at-band [20, 30]; `aboveCeilingMultiplier: 1.95` → above-band ceiling 39 (strictly under the 41-call critical-escalation boundary so above-leg cannot ambiently bracket the critical tier). Capture screen uses parallel `Future.wait` batches (sequential awaits cap effective send rate ~12-80/sec on iOS wireless, making above band unreachable), per-leg `debugProfilePlatformChannels = true` in try/finally so framework flag does not leak into post-leg live monitoring, channel reuses the existing `sleuth_demo_channel` registered at `example/ios/Runner/AppDelegate.swift:21-25` (handler returns nil), no stream narrowing (3.2s scenario span — alloc 1.5s + dwell 1.5s + barrier 200ms — well inside ring buffer; existing 3 captures predate the 800→1500ms dwell bump and remain schema-valid per fixture-provenance note). New audit anchor block with critical-tier + duration-axis prose-drift guards. 2,835 tests passing (+7 net from v0.19.3). `fvm flutter analyze` clean.

**v0.19.3** — `MemoryPressureDetector.heap_growing` raised to **runtimeVerified** via `perStableIdTier` (warning tier, 512 KB/s sustained ≥10s). Distribution: **21/23 reproducerOnly base, 2/23 effective runtimeVerified families** counting per-family raises (HeavyCompute.heavy_compute base + MemoryPressure.heap_growing per-family). Other 3 MemoryPressure families (gc_pressure, heap_near_capacity, native_memory_growing) stay base reproducerOnly. New public API: `Sleuth.suspendNonEssentialTimelineStreams()` / `Sleuth.resumeAllTimelineStreams()` narrow VM timeline streams to `Dart` only during scenario spans so 30s heavy-allocation phases don't overflow the ring buffer. `SleuthController.resetCaptureState()` now also resets `_memoryPressure` so heap_growing regression starts fresh on scenario allocation only. `ProfileCaptureSchema.approvedUnits` adds `bytes/sec`. New `MemoryPressureCaptureScreen` example app screen with calibration + retry + stash-then-export + post-capture validator. `vmConnected = false` setter on `MemoryPressureDetector` now clears all identity-bearing state. `PerformanceIssue.copyWith` adds `dedupIdentityMicros` parameter (silent-drop fix). `SleuthController.exportCaptureJson` adds debugPrint diagnostics at each null-return path. 2,828 tests passing (+6 net from v0.19.2). `fvm flutter analyze` clean.

**v0.19.2** — Polish release. No new tier raises, no public API changes, no BREAKING. Closes v0.19.1 deferrals: (1) `_sweepStalePendingBegins` extended to evict stale `_lastProcessedTsByTid` cursors **in non-capture polling mode only** (`!retainTimeline`; new `_cursorMaxIdleMicros = 30s` constant) so long-lived sessions with churning tids don't accumulate cursors indefinitely. Capture mode (`retainTimeline=true`) retains cursors for the session lifetime — the VM buffer is intentionally re-read across polls and the cursor IS the dedup mechanism preventing replay of retained events. Pending-begins age sweep stays unconditional in both modes (orphan B is a leak regardless of buffer-retention strategy). (2) 2 new Layer-3 NetworkMonitor time-window tests pin `_frequencyWindowMs=5000` boundary (5+5 records 6s-spread silent for `request_frequency`; 2+2 errors 6s-spread silent for `http_error_spike`); (3) `.gitattributes` rule for `test/validation/captures/**/*.json` (`-text -diff linguist-vendored=true`) prevents Windows-CRLF byte drift + collapses fixture JSONs in PR diffs. Note: cursor sweep skipped on idle polls (no events → no anchor ts) and `.gitattributes` cannot block IDE auto-format on save — SHA pin remains the safety net.

**v0.19.1** — Hardening release. No new tier raises, no public API changes, no BREAKING. Parser gains `cursorsByTid` per-tid composite cursor (`TimelineCursor = ({int lastTs, Set<String> seenSignatures})`) threaded through `VmServiceClient._lastProcessedTsByTid` so capture-mode buffer re-reads dedup at parse-loop entry — fixes RebuildDetector / MemoryPressure / PlatformChannel false-positive inflation in capture mode. Signature is `'$ph|$name|${id ?? ""}'` — distinguishes same-microsecond events with different names or async ids; protocol-indistinguishable same-name sync events still conflated correctly. `VmServiceClient` adds `_sessionGeneration` int field bumped in `_cleanup()`; `_pollTimeline` re-checks generation after each await before writing session state, firing callbacks, or running `_sweepStalePendingBegins` — prevents stale in-flight polls from corrupting new-session state across reconnects. NetworkMonitor Layer-3 tests replaced with boundary triads pinning `>` vs `>=` per family. `checkPerStableIdTier` rejects empty `{}` map. `DetectorMetadata.perStableIdTier` doc adds convention note: single-family detectors set base `tier` directly (HeavyCompute pattern unchanged — perStableIdTier is for multi-family).

**v0.19.0** — Hardening release. **BREAKING**: `sleuthMetadata.role` (`'below'|'at'|'above'`) is now a required capture field; pre-v0.19.0 captures fail to parse with `Missing required sleuthMetadata key: "role"`. Minor-bump per pre-1.0 SemVer convention. No new tier raises. Detector-row distribution: **22/23 `reproducerOnly`, 1/23 `runtimeVerified`, 0/23 `unvalidated`** because `NetworkMonitorDetector`'s base tier dropped from `runtimeVerified` to `reproducerOnly` — the `slow_request` evidence is preserved via the new `DetectorMetadata.perStableIdTier` raise (effective per-family tier unchanged: `slow_request` still ships at runtimeVerified, audit gate's `effectiveMaxTier` still routes to the runtimeVerified branch and re-runs `validateBracket`). Four other emitted families (`large_response`, `request_frequency`, `http_error_spike`, `high_frequency_same_path`) are no longer mechanically over-claimed by the detector-level tag. HeavyCompute (single-family) keeps base `runtimeVerified`. Other v0.19.0 changes: (1) cross-batch BUILD `B`/`E` reconstruction in `TimelineParser` via per-tid pending-begins map threaded through `VmServiceClient._pendingBuildBegins` (cap 100 per tid, oldest-drop) — fixes BUILDs falling on poll boundaries from being silently dropped; (2) structural `role: 'below'|'at'|'above'` field threaded through `Sleuth.exportCaptureJson` → `sleuthMetadata.role` → `ProfileCaptureSchema` (replaces the v0.18.2 `_below.json` filename heuristic); (3) `--role` CLI flag on `tool/wrap_capture.dart` with filename-derived fallback; (4) deprecated `pollTimelineForTest` alias removed; (5) audit gate routes off `effectiveMaxTier` not `meta.tier` so per-family raises trigger the right branch; perStableIdTier invariants (keys ⊆ coveredStableIds, values ≥ base tier, bracketStableId effective tier) hoisted out of the unvalidated-base guard via new `checkPerStableIdTier` helper to close the audit-bypass where a base-`unvalidated` detector could mint runtimeVerified evidence; (6) `expectingNoEmission` shim parameter on `parse` annotated `@visibleForTesting` — role field is canonical.

**v0.18.2** — Second `runtimeVerified` raise: `HeavyComputeDetector.heavy_compute` (warning tier, 8 ms threshold). First vmOnly raise enabled by v0.18.1's `Sleuth.flushTimelineNow()`. Three on-device captures (iPhone 12 / iOS 17.5 / Flutter 3.41.x) recorded under producer-side dedup with stable per-BUILD `detectedAt`. v0.18.2 extended `TimelineParser` to reconstruct `BUILD` `dur` from `ph: 'B'`/'E'` pairs (iOS profile mode emits B/E only, no X-form BUILDs).

**v0.18.1** — Hardening release fulfilling the v0.18.0 commitment. Shipped: (1) `Sleuth.flushTimelineNow({Duration? timeout})` public API — synchronous VM poll + emission drain, unblocks every other vmOnly tier raise; (2) producer-side dedup in `SleuthController._recordIssuesForCapture` (composite key `<detectorType>|<stableId>|<severity>|<detectedAtMicros>`); (3) `SleuthController.resetCaptureState()` cleared automatically by `Sleuth.markScenarioBegin` so multi-leg flows no longer leak across legs; (4) opt-in `requireUniqueDetectedAtMicros: true` in `ProfileCaptureSchema.validateBracket` for v0.18.2+ captures recorded under producer dedup; (5) `pollTimelineForTest` renamed `pollTimelineSync` (deprecated alias kept for one release); (6) audit-gate regression-guard test pinning `validateBracket(... requireTraceRecord: true)` for every runtimeVerified detector; (7) `doc/capture_procedure.md` updated with the vmOnly `flushTimelineNow` pattern + multi-leg recovery fallback. v0.18.2+ first vmOnly raise (HeavyCompute likely) refreshes NetworkMonitor's triad and flips both metadata to opt into the strong invariant.

**v0.18.0** — Ledger distribution: **22/23 `reproducerOnly`, 1/23 `runtimeVerified`, 0/23 `unvalidated`**. `NetworkMonitorDetector.slow_request` (warning tier, 1000 ms threshold) is the first runtimeVerified raise — three on-device captures (iPhone 12 / iOS 17.5 / Flutter 3.41.x) recorded via the new in-app capture procedure (markScenarioBegin/End + Sleuth.exportCaptureJson + iOS clipboard). Phase A capture infrastructure shipped: scenario markers, exportCaptureJson, retainTimeline gate, schemaVersion v1, severity-scoped trace records, above>at invariant, severity-boundary cross-check.

**v0.17.6** — Ledger distribution: **23/23 `reproducerOnly`, 0/23 `unvalidated`**. No detectors at `runtimeVerified` or `externallyCited`. Tier-quality audit COMPLETE — evidence ships in two within-tier strata (the third "reused unit-test suites" stratum is now empty):
1. **Parser-boundary exercised** (8 detectors): v0.17.4 ShaderJank, HeavyCompute, PlatformChannel, MemoryPressure (vmOnly batch); v0.17.5 GpuPressure, ShallowRebuildRisk (hybrid batch); v0.17.6 Repaint, Rebuild (final hybrid batch — all paths exercised in one file via cross-harness composition).
2. **Real `pumpWidget` + `scanTree`** (13 structural detectors, v0.16.3 / v0.17.1 batches).

Note: Stratum 1 covers the 8 v0.17.2-batch detectors that originally pointed at `test/detectors/*_detector_test.dart` reused unit tests. Stratum 2 (13 structural) detectors still legitimately use real `pumpWidget` for their primary path; they are NOT a regression — the structural lifecycle has no parser boundary to exercise.

**v0.17.6** closed the parser-boundary gap for the final 2 hybrid detectors (Repaint + Rebuild). Repaint reproducer pins all 3 emission paths plus broad **Gate B animation-owned suppression** via `expect(issues, isEmpty)` (regression cannot leak through any of VM aggregate, per-widget, or aggregate-debug). Rebuild reproducer pins **builder-widget 3× threshold multiplier** with two pieces of evidence — full builder triad on `StreamBuilder` (29/30/91) AND paired non-builder/builder fixture at identical rate=25 (`MyWidget` fires at `> 10`, `StreamBuilder` suppressed at `< 30`). **Source-mode `RebuildCountSource.flutterTimeline`** per-type suppression pinned (KDD-5 inflations) with default-source negative control. Both detectors pin reconnect-flush behaviour (cold-init `vmConnected=false → true` stages `_pendingVmWindowCount=0`) and `_maxHighlightsPerType=3` cap (5 instances → 3 highlights). Single-call window-staging pattern: advance fake clock by 1100 ms BEFORE `processTimelineData` so the call closes the window atomically.

**v0.17.5** closed the parser-boundary gap for 2 hybrid detectors (GpuPressure + ShallowRebuildRisk). Cross-harness composition pattern: hybrid reproducers import both `vm_reproducer_harness.dart` (for `parseAndAssertShape`) and `structural_reproducer_harness.dart` (for `scanAndIssues`). VM state survives across `scanAndIssues` because `prepareScan` only clears per-scan accumulators, not VM-window state. ShallowRebuildRisk reproducer pins all 3 gate states including the silent `vmConnected=true && buildCount ≤ 20` no-fire branch + DebugSnapshot upgrade in BOTH duplicated `_evaluate` branches (VM-backed + structural-fallback). GpuPressure reproducer pins RenderOpacity opacity-value short-circuit + BackdropFilter sigma 3-band (highlight severity escalates critical at >10.0; `expensive_gpu_nodes` issue severity stays `warning`) + confidence correlation matrix (4 sub-cases including raster-present-but-non-dominant). Detector rationales enumerate 4 RenderObject checks + 1 widget-level (`ColorFiltered`) check explicitly.

**v0.17.4** closed the parser-boundary gap for 4 of 8 v0.17.2-batch detectors. New shared harness `test/validation/_helpers/vm_reproducer_harness.dart` with parser-drop guard (`parseAndAssertShape`). MemoryPressure rationale discloses 3 skipped upstream hops (`VmServiceClient.getMemoryUsage` repack, `EventStreams.kGC` stream, `VmServiceClient._readRssBytes()` / `ProcessInfo.currentRss`). Anchor block `_v0172Expectations` → `_v0174Expectations`.

**v0.17.3** closed the audit methodology gap that forced v0.17.2 to ship two partial-coverage narrowings. Added `DetectorMetadata.parametricFamilies: Set<String>?` as peer namespace to `coveredStableIds` — matcher credits `<family>_<non-empty-suffix>` literals. `RepaintDetector` + `RebuildDetector` drop narrowings; all 3 underscore-parametric families (`repaint_debug`, `rebuild_debug`) declared + audit-tracked. Literal-provenance matcher hardened via 8-point structural provenance model (sticky-binding release, subtree-taint boundary, Rule-1 shadow detection, over-bound `fold`/`reduce` closure params). 12 regression fixtures.

**Next forward motion** — `reproducerOnly` → `runtimeVerified` raises. Need checked-in profile-mode captures (timeline JSON / DevTools snapshot) showing claimed magnitude on reference device. NetworkMonitor has 3 capture files orphaned on disk (`consumeBy: '0.18.0'`) — lowest-friction candidate but blocked on (a) replacing NN/g citation, (b) extending capture helper to emit `sleuth.issue.slow_request.warning` trace record, (c) extending `ProfileCaptureSchema.validateBracket` to require it. Other candidates: any of the 6 Stratum-1 vmOnly/hybrid detectors with deterministic timeline footprint (HeavyCompute and ShaderJank are simplest — single-stableId, single-axis thresholds).

**NetworkMonitor re-raise prerequisites** (deferred to v0.18.0): (1) replace NN/g citation (rejected twice — UI direct-manipulation guideline, not generic HTTP latency threshold); (2) extend capture helper to emit `sleuth.issue.slow_request.warning` trace record with detector-measured duration; (3) extend `ProfileCaptureSchema.validateBracket` to require that record inside scenario window.

For version history v0.17.4 and earlier see `CHANGELOG.md`. Spec docs in `doc/spec_v*.md`.
